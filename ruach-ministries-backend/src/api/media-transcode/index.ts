import type { Core } from "@strapi/strapi";

export default {
  register({}: { strapi: Core.Strapi }) {
    // Register plugin/extension
  },

  bootstrap({}: { strapi: Core.Strapi }) {
    // Bootstrap plugin/extension
  },
};
