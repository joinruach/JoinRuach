{
  "kind": "collectionType",
  "collectionName": "edit_decision_lists",
  "info": {
    "singularName": "edit-decision-list",
    "pluralName": "edit-decision-lists",
    "displayName": "Edit Decision List",
    "description": "AI-generated multi-camera edit plans with camera switches, chapters, and operator review workflow"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "edlId": {
      "type": "uid",
      "required": true,
      "unique": true,
      "targetField": "edlId"
    },
    "session": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::recording-session.recording-session",
      "inversedBy": "edl"
    },
    "version": {
      "type": "integer",
      "default": 1,
      "min": 1,
      "required": true,
      "description": "EDL version number for tracking iterations"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "draft",
        "reviewing",
        "approved",
        "locked"
      ],
      "default": "draft",
      "required": true,
      "index": true,
      "description": "Workflow status: draft → reviewing → approved → locked (ready for render)"
    },
    "source": {
      "type": "enumeration",
      "enum": [
        "ai",
        "operator",
        "hybrid"
      ],
      "default": "ai",
      "required": true,
      "description": "How the EDL was generated: AI rules, manual operator edits, or combination"
    },
    "timebase": {
      "type": "string",
      "default": "ms",
      "required": true,
      "maxLength": 10,
      "description": "Time unit for all timestamps (milliseconds)"
    },
    "fps": {
      "type": "integer",
      "min": 1,
      "max": 120,
      "description": "Frames per second if frame-based timing is needed"
    },
    "canonicalEdl": {
      "type": "json",
      "required": true,
      "description": "The canonical EDL JSON - single source of truth for all edit decisions"
    },
    "exports": {
      "type": "json",
      "default": {},
      "description": "Generated export file URLs and metadata (fcpxml, premiere, resolve, json)"
    },
    "audit": {
      "type": "json",
      "default": {},
      "description": "Generation metadata: prompt, model, temperature, tool versions, transcript hash, assets hash"
    },
    "metadata": {
      "type": "json",
      "default": {},
      "description": "Additional metadata including review notes, operator comments, generation timestamp"
    }
  }
}
